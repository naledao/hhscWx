<van-collapse value="{{ activeNames }}" bind:change="onChange" bindtap="flag">
  <van-collapse-item wx:for="{{about}}" wx:key="id" title="{{item.title}}" name="{{item.id}}">
    {{item.content}}
    </van-collapse-item>
</van-collapse>

